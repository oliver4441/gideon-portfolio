{
  "name": "gideon-portfolio",
  "version": "2.0.1",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/enhance-background.mjs && astro build && node scripts/postbuild-contrast.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/vercel": "^8.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
